# GNUCMS 작업 지침

이 파일은 저장소 전체에 적용한다. 더 하위 디렉터리에 별도의 `AGENTS.md`가 있으면 그 파일의 지침이 우선한다.

## 프로젝트 개요

- PHP 8.2 이상에서 동작하는 Slim 4 기반 CMS다. 운영 환경은 PHP 8.4를 권장한다.
- 애플리케이션 코드는 `src/`, 기본 화면은 `templates/default/`, 공개 자산과 진입점은 `www/`에 있다.
- SQLite와 MySQL/MariaDB를 지원한다. DB 관련 변경은 두 DB 계열에서 같은 의미로 동작해야 한다.
- `config/config.php`, `storage/`의 런타임 데이터와 `vendor/`는 로컬·배포 환경의 파일이며 저장소에 커밋하지 않는다.

## 배포 환경

- 프로덕션 서버에서는 `composer install`, `npm install`, `npm run build` 등의 의존성 설치나 빌드 작업을 하지 않는다.
- 배포본은 개발 환경이나 CI에서 미리 완성하며, 실행에 필요한 `vendor/`와 정적 자산을 포함해야 한다.
- 새 의존성이나 프런트엔드 도구를 도입할 때도 운영 서버에 Composer, Node.js, npm 또는 컴파일러가 있다고 가정하지 않는다.
- 빌드가 필요한 변경은 빌드 결과물을 배포본에 포함하거나, 가능하면 별도 빌드가 필요 없는 정적 자산으로 제공한다.
- 운영 중 애플리케이션이 쓸 수 있다고 가정할 경로는 기본적으로 `config/`와 `storage/`뿐이다.

## 현재 기능 지도

- 설치·업그레이드: 웹 설치, 서버 요구사항 점검, 멱등 DB 마이그레이션, SQLite 자동 백업, DB·업로드·설정 전체 수동 백업과 SQLite 복원, CLI 및 점검 화면을 제공한다.
- 게시판·콘텐츠: 게시판별 권한·관리자·분류·공지·비밀글·첨부 파일과 목록형·갤러리형·매거진형·뉴스형 보기를 지원한다.
- 글·댓글: 회원과 비회원 작성·수정·삭제, 비회원 비밀번호 확인, 에디터 이미지, 게시판별 게시글·댓글 검색, 작성자별 글·댓글 조회를 지원한다.
- 회원·인증: 이메일 가입·인증·비밀번호 재설정, Google·네이버·카카오 로그인, 계정 연결, 프로필 이미지, 탈퇴를 지원한다.
- 운영·보안: 관리자 회원·로그인 기록 관리, SMTP·OAuth 설정 암호화, 글·댓글 및 로그인 시도 제한, Cloudflare Turnstile을 지원한다.
- 사이트 관리: 일반 문서, 약관·개인정보 문서와 동의 기록, 알림, 사이트맵·robots.txt·RSS, 사이트 소유 확인·분석·광고 설정을 제공한다.
- 화면 확장: `templates/default/` 기본 테마와 테마별 템플릿 재정의를 지원하며 정적 자산에는 내용 해시 버전을 붙인다.
- 확장 관리: 관리자에서 플러그인과 모듈을 별도로 조회하고 사용 여부를 토글한다. 패키지 설명 검증, 필수·선택 의존성 처리와 활성 패키지의 서비스·라우트 등록을 지원한다. 패키지별 짧은 기본 주소와 공개 메뉴 연결, 기존 주소의 POST·콜백 호환, 경로 충돌 검사를 제공한다. 확장 운영 화면은 관리자 공통 메뉴·글꼴·폼·버튼·표와 다크 모드를 사용하며 테마의 `extensions/<이름>/`에서 화면·조각을 재정의한다.
- 확장 운영 기반: 인증된 외부 JSON·폼 콜백, 명시적 패키지 DB 설치·갱신, 확장 테이블·활성 상태 백업과 복원 시 외부 실행 허용 해제를 지원한다.
- 알림톡: 비즈뿌리오 플러그인과 알림톡 운영 모듈로 환경별 암호화 설정, 모듈 연동/웹발송 계정 유형과 API 인증 확인, 기본 텍스트·웹링크 승인 템플릿 수동 등록, 관리자 단건 웹발송, 중복 방지, 웹훅 결과·재요청·이력과 보관 만료 개인정보 정리를 제공한다. 비즈뿌리오 사이트 웹발송 진입 경로를 제공하며, 웹발송 계정의 GNUCMS 발송에는 API 사용 권한이 필요하다. KAPI 동기화·문자 대체·업무 자동 발송·사이트 발송 이력 동기화는 포함하지 않는다.
- 결제·쇼핑몰: PG 직접 연동을 사용하는 1인 운영 쇼핑몰이다. 이니시스 PC·모바일 승인·INIAPI 조회·환불과 토스페이먼츠 카드·간편결제 승인·조회·전체/부분 환불 코드를 제공하며 운영 전 실거래 검증이 필요하다. KCP·KSPay는 미확인 규격이 남아 결제 선택을 차단한 준비 단계다. `/shop` 주소에서 사이트 공통 테마·메뉴·다크 모드에 연결된 사용자 상품 검색·상세·장바구니·주문서·내 주문, 로그인 후 주문 복귀, 회원 주문, `/admin/shop` 아래 상품 목록·등록·수정 분리, 옵션 두 종류·등록부터 조합별 판매가·원가·재고 입력과 아래 행 일괄 복사·동시 변경 검사, 등록·수정 시 상품·옵션·다중 이미지 통합 저장, 이미지 미리보기·드래그 순서 조정·대표 이미지와 상세 갤러리, CKEditor 상품 설명·본문 사진, 배송, 취소·반품·전체/부분 환불·동일 가격 교환, 수량 원장과 입금 대조·CSV 정산, 주문 당시 원가 보존과 취소·반품·교환을 반영한 상품별 원가·이익 집계를 제공한다. 리셀러 코드는 가맹점 등록에 쓰고 결제 전문에는 상점 코드를 사용한다.
- 확장 예제: 메시지 형식 플러그인과 예약 안내문 모듈의 관리자 전용 데모를 제공한다. 독립 실행과 선택 서비스 연동을 보여주며 실제 예약 저장·외부 발송은 하지 않는다. 예약 안내문은 실행 바로가기와 미사용 상태의 관리자 테스트를 지원한다.

이 목록은 작업 이력이 아니라 현재 구현 범위를 설명한다. 기능을 추가하거나 제거해 범위가 달라지면 함께 갱신하고, 릴리스별 변경 내역은 `CHANGELOG.md` 또는 GitHub Release에 기록한다.

## 작업 원칙

- 기존 작업 트리가 더러울 수 있으므로 관련 없는 변경을 덮어쓰거나 되돌리지 않는다.
- 기능을 바꿀 때는 컨트롤러만 고치지 말고 서비스·저장소·템플릿·테스트 사이의 책임을 확인한다.
- 사용자 입력은 서버에서 다시 검증하고, 화면 출력은 템플릿의 이스케이프 도우미를 사용한다.
- 비밀번호, OAuth 비밀키, SMTP 비밀번호, Turnstile 비밀키 등 민감정보를 코드·테스트 fixture·로그에 남기지 않는다.
- 공개 화면과 관리자 화면 변경 시 기본 테마뿐 아니라 테마 재정의와 공통 partial에 미치는 영향도 확인한다.

## 데이터베이스 변경

- 제품 릴리스 버전과 `src/Db/Schema.php`의 `Schema::VERSION`은 별개다. `Schema::VERSION`은 DB 구조 판 번호로만 사용한다.
- 테이블이나 컬럼을 바꾸면 새 설치용 DDL과 기존 설치용 멱등 마이그레이션을 함께 수정한다.
- 실제 구조 변경이 있을 때만 `Schema::VERSION`을 올린다.
- 마이그레이션은 중간 실패 후 다시 실행해도 안전해야 하며 기존 데이터를 보존해야 한다.
- DB 테스트에는 가능한 경우 `connectionProvider`를 사용한다.

## 테스트와 검증

- 개발·CI 의존성 설치: `composer install`
- 관련 테스트: `./vendor/bin/phpunit tests/경로/관련테스트.php`
- 전체 테스트: `./vendor/bin/phpunit`
- PHP 문법 검사: `php -l 파일.php`
- 완료 전 관련 테스트를 먼저 실행하고, 공통 코드·DB·라우팅을 건드렸다면 전체 테스트도 실행한다.
- 기존 경고를 작업 범위 밖에서 임의로 숨기지 말고 새 경고나 deprecation을 추가하지 않는다.

## Git과 릴리스

- 커밋은 한 가지 논리 변경을 담고 `feat:`, `fix:`, `refactor:`, `docs:`, `test:`, `chore:` 형식을 사용한다.
- `main`은 배포 가능한 상태로 유지하고, 큰 작업은 짧게 유지되는 기능 브랜치에서 진행한다.
- 제품 버전은 SemVer(`MAJOR.MINOR.PATCH`)로 관리한다. 호환성 파괴는 MAJOR, 하위 호환 기능은 MINOR, 버그 수정은 PATCH를 올린다.
- 제품 버전의 원본은 루트 `version.txt`다. 직접 수정하지 않고 Release Please가 릴리스 PR에서 갱신하게 한다.
- 정식 릴리스는 Release Please가 만든 `vX.Y.Z` 태그로 표시한다. 같은 이름의 GitHub Release에는 사용자 관점의 변경사항과 업그레이드 주의를 기록한다.
- Release Please가 만든 릴리스 PR을 병합하면 태그·GitHub Release·배포 ZIP이 생성된다. 자동화 파일을 수정할 때도 프로덕션 빌드를 요구하지 않는다.
- 배포용 버전과 DB 스키마 판 번호를 맞추려 하지 않는다. 한 제품 릴리스에 여러 스키마 변경이 포함될 수 있고, 스키마 변경 없는 릴리스도 가능하다.
- 릴리스 전 전체 테스트를 통과시키고 지원 DB의 마이그레이션, 새 설치, 주요 로그인·글쓰기 흐름을 점검한다.

## 완료 기준

- 요청한 동작과 오류 경로가 모두 구현되어 있다.
- 변경에 맞는 회귀 테스트가 추가되거나 수정되어 있다.
- 관련 문서와 샘플 설정이 실제 동작과 일치한다.
- `git diff --check`가 통과하며 민감정보와 런타임 파일이 포함되지 않는다.
